import type { Metadata } from "next";
import { Geist, Geist_Mono, Noto_Sans_Ethiopic, Playfair_Display } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { Toaster as SonnerToaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const notoEthiopic = Noto_Sans_Ethiopic({
  variable: "--font-noto-ethiopic",
  subsets: ["ethiopic"],
  weight: ["400", "500", "600", "700"],
});

const playfairDisplay = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "PCC Professional Competence Certificate Portal | Ethiopia",
  description:
    "Official portal for the Innovation and Technology Agency (PCC) Professional Competence Certificate process. Apply for ICT sector business licenses in Ethiopia (Harari Region).",
  keywords: [
    "PCC",
    "Ethiopia",
    "Harari",
    "ICT license",
    "Professional Competence Certificate",
    "business license",
    "Directive 1/2007",
  ],
  authors: [{ name: "Innovation and Technology Agency" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} ${notoEthiopic.variable} ${playfairDisplay.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
        <SonnerToaster richColors position="top-right" />
      </body>
    </html>
  );
}
